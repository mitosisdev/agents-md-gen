{
  "name": "full-pkg",
  "version": "2.3.4",
  "description": "A full-featured package",
  "main": "src/index.ts",
  "bin": {
    "full-cli": "bin/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun run build.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
